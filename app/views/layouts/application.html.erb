<!DOCTYPE html>
<html>
  <head>
    <title>Lab11</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
        tr, th, td {
            border: 1px solid black;
            }
        .tab{
            height:40%;
            width:100%;
            border: 1px solid black;
            }
    </style>
  </head>
      <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
        <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"/></svg>
        <span class="fs-4"><strong>CodeIgor31</strong></span>
        <nav class="my-2 my-md-0 mr-md-3">
        <span class="nav-item"><strong><%= link_to "Страница проверки", check_path %></strong></span>
        <span class="nav-item"><strong><%= link_to "Главная страница", home_path %></strong></span>
        </nav>
      </a>
    </header>
  <body>
  <div class = "container">
    <% flash.each do |type, msg| %>
      <div class='alert alert-info'>
        <%= msg %>
      </div>
    <% end %>
    <%= yield %>
    </div>
  </body>
</html>